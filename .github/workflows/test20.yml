name: Cache2

on:
    push:
jobs:
    my-new-cache-workflow:
        runs-on: ubuntu-latest
        steps:
            - name: Fetch code
              uses: actions/checkout@v4
            - name: Install node 18
              uses: actions/setup-node@v4
              with:
                node-version: '18'